# Examples

## Mocked offline demo

Run from the repository root:

```bash
python examples/mock_demo.py
```

The demo uses a deterministic in-process mock backend, so it does not require Ollama, OpenAI, Gemini, network access, API keys, webcam access, or memory files. It writes `examples/mock_tree.dot` to show the behavior-tree structure.
